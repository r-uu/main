# ============================================================================
# MicroProfile Config for JDBC PostgreSQL Library Tests
# ============================================================================
# Properties are injected by Maven resource filtering from config.properties
# Maven replaces ${db.xxx} with actual values during build
# See: config.properties (machine-specific, git-ignored)
#
# Fallback values (after colon) are used if Maven filtering doesn't work
# ============================================================================

config_ordinal=100

# PostgreSQL Database Connection
# Format: ${maven.property:fallback-value}
# Maven tries to replace ${postgres_jeeeraaah_host}, if that fails, uses "localhost"
database.host=${postgres_jeeeraaah_host:localhost}
database.port=${postgres_jeeeraaah_port:5432}
database.name=${postgres_jeeeraaah_database_lib_test:lib_test}
database.user=${postgres_jeeeraaah_user:postgres_jeeeraaah_username}
database.password=${postgres_jeeeraaah_password:postgres_jeeeraaah_password}

