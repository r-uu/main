# root

maven parent module for several maven child modules

- imports bill of material and thereby makes common dependencies available for maven child modules
- child modules
  - lib, contains libraries that might be reused by other maven modules
  - app, contains applications